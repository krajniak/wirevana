{
  "name": "@wirevana/cli",
  "version": "0.0.1",
  "description": "Wirevana CLI for building ChatGPT Canvas bundles.",
  "type": "module",
  "bin": {
    "wirevana": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --no-splitting --target es2020 --out-dir dist"
  },
  "license": "MIT",
  "dependencies": {
    "@wirevana/renderer": "workspace:*",
    "@wirevana/runtime": "workspace:*",
    "tsup": "^7.2.0"
  }
}
